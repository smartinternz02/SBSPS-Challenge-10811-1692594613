import React from 'react';
import Nav from './Nav'; // Make sure the paths are correct
import Hero from './Hero'; // Make sure the paths are correct
import Features from './Features'; // Make sure the paths are correct

const Dashboard = () => {
  return (
    <div>
      <Nav />
      <Hero />
      <Features />
    </div>
  );
}

export default Dashboard;
